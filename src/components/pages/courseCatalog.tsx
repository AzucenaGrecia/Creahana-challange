import React from "react"
import Hearder from "../UI/header"
import Banner from "../UI/banner"

export default function CourseCatalog() {
  return (
    <div>
      < Hearder />
      < Banner />
    </div>
  )
}